{
  "name": "dssgreenhash-frontend",
  "version": "1.0.0",
  "description": "Frontend for DSS GreenHash app",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "clean": "rm -rf node_modules && npm install",
    "build": "expo build"
  },
  "dependencies": {
    "expo": "^46.0.0",
    "react": "17.0.1",
    "react-native": "0.64.2"
  },
  "devDependencies": {
    "@babel/core": "^7.12.9"
  }
}
